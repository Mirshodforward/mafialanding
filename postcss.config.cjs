// Local (empty) PostCSS config. The landing uses plain CSS — this shields it
// from the parent Mafia-online project's Tailwind/PostCSS config, which PostCSS
// would otherwise discover by walking up the directory tree.
module.exports = {};
